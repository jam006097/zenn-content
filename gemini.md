# Zenn記事執筆規約

## 1. はじめに
このドキュメントは、Zennに記事を投稿する際のガイドラインを定めます。記事の品質を統一し、執筆作業を効率化することを目的としています。

## 2. ファイル管理規約
### 2.1 記事ファイル
- `articles` ディレクトリに配置してください。
- ファイル名は、記事のURLスラッグとなるように `[slug].md` の形式で記述してください。（例: `how-to-use-gemini-cli.md`）

### 2.2 画像ファイル
- `images` ディレクトリ内に、記事のファイル名（スラッグ）と同じ名前のサブディレクトリを作成し、その中に画像を配置してください。
    - 例: `images/how-to-use-gemini-cli/image1.png`
- 画像ファイルの命名規則は、半角英数字とハイフンのみを使用してください。

## 3. 記事のメタ情報 (Frontmatter)
各記事ファイルの先頭に、以下のFrontmatterを記述してください。

```yaml
---
title: "記事のタイトル" # 記事のタイトルを記述
emoji: "✨" # アイキャッチとなる絵文字を一つ記述
type: "tech" # "tech" (技術記事) または "idea" (アイデア記事) のいずれかを記述
topics: # 関連するタグを記述（推奨3〜5個、最大5個まで）
  - "gemini"
  - "cli"
  - "markdown"
published: false # 公開する場合は true, 下書きの場合は false
---
```

- `title`: 記事のタイトルを記述します。
- `emoji`: 記事一覧やSNSシェア時に表示されるアイキャッチとなる絵文字を一つ記述してください。
- `type`: 記事の種類を指定します。
    - `tech`: プログラミングや技術解説など、技術的な内容を扱います。
    - `idea`: 考え方、ノウハウ、学習方法など、アイデアや知見を共有する内容を扱います。
- `topics`: 記事の内容に関連するタグを記述してください。推奨は3〜5個で、最大5個までとします。
- `published`: 記事の公開状態を制御します。
    - `true`: 記事は公開されます。
    - `false`: 記事は下書き状態となり、公開されません。

## 4. 執筆スタイル・フォーマット
記事本文の品質を保つため、以下のルールを遵守してください。

### 4.1 文体
- 記事全体の文体は「ですます調」に統一してください。

### 4.2 表記
- 英数字は半角、カタカナは全角を使用してください。
- よく使われる技術用語や固有名詞（例: `Gemini`, `JavaScript`, `VS Code`, `GitHub`など）は、公式表記に従い、表記ゆれがないようにしてください。

### 4.3 Markdown記法
- 見出しは `#` (h1) を記事タイトルに使用し、本文中のセクションには `##` (h2) から使用してください。
- コードブロックを記述する際は、必ず言語指定を行ってください。
    - 例:
    ```typescript
    console.log("Hello, Zenn!");
    ```
- Zenn独自の記法（メッセージブロック、アコーディオンなど）の積極的な活用を推奨します。
- 

---

## 図表（Mermaid 記法）に関するベストプラクティス

### Zenn での対応状況
- Zenn は Mermaid 記法（```mermaid）に対応している
- フローチャート、シーケンス図、クラス図、ER図などが利用可能

---

### Mermaid を使う目的
- 文章だけでは伝わりにくい構造や流れを補助するために使う
- 図を主役にせず、文章理解を助ける補助情報として扱う

---

### 記述ルール
- 図は「何を表しているか」が一目で分かる最小構成にする
- ノード名・ラベルは日本語または分かりやすい英語を使う
- 1図1メッセージを原則とし、複雑になりすぎないようにする

---

### 図を入れる判断基準
- 処理の流れを時系列で説明したい → sequenceDiagram / flowchart
- 構造・責務・関係性を示したい → classDiagram / erDiagram
- 文章で3回以上「流れ」「構造」と書いている場合は図を検討する

---

### 注意点
- Zenn のプレビューで必ず表示確認を行う
- Mermaid のバージョン差異による記法エラーに注意する
- ダークモードでも可読性が保たれる構成を意識する

---

### NG例
- 図だけで説明が完結している（文章がない）
- 一つの図に情報を詰め込みすぎている
- 図の意図が本文中で説明されていない
## 5. AIによる記事生成ガイドライン
AI（Gemini）を活用して記事を生成する際は、以下のガイドラインに従うことで、品質の安定化と効率化を図ります。

### 5.1 基本的なプロンプト構成
記事生成を依頼するプロンプトには、最低限以下の要素を含めるようにしてください。

#### **役割 (Role)**
- あなたは経験豊富なソフトウェアエンジニアです

####  **目的 (Goal)**
- Zennに投稿するための技術記事を作成してください

#### **対象読者 (Audience)**
- 実務で開発をしているエンジニア
- 初学者向けではないが、前提知識は明示する
- 「なぜそうするのか」を知りたい層

#### **トーン＆マナー (Tone & Manner)**
 専門用語を避け、図や例えを多用して分かりやすく解説してください``

#### 構成の原則
- 結論 → 理由 → 具体例 → まとめ の流れを基本とする
- 見出し（## / ###）を適切に使い、スクロールしても把握できる構成にする
- 1セクション1テーマを守る

#### 内容に関する指針
- 「何ができるか」だけでなく「なぜそれが必要か」を説明する
- トレードオフがある場合は必ず触れる
- 個人的な経験や判断は、その前提条件を明示する
- 用語や略語は、初出時に簡潔に補足する

#### コード・技術説明
- コードは最小構成で、意図が分かるものを載せる
- なぜその書き方をしているのかを文章で補足する
- 「動くこと」だけでなく「読みやすさ・保守性」にも言及する
- テストや設計の話題では、背景にある考え方を重視する

#### Zenn向けの注意点
- 記事タイトルは「何が分かるか」が一目で伝わるものにする
- 箇条書きと文章を適度に混ぜ、読み疲れを防ぐ
- まとめでは「この記事で伝えたかったこと」を明示する

#### NG例
- 結論のない長文
- 前提知識が暗黙のまま進む説明
- 「とりあえず動く」だけで理由のないコード

#### **出力形式 (Format)**: ZennのMarkdown形式で、コードブロックやリストなど適切なフォーマットを用いるよう指示します。

### 5.2 プロンプトのテンプレート例
以下は、記事の骨子作成を依頼する際のプロンプトのテンプレートです。

```
あなたは、Zennで多くの読者から支持されているテクニカルライターです。

以下のテーマに関する技術記事の構成案（目次）を作成してください。

# テーマ
（ここに記事のテーマを記述）

# 想定読者
（ここに想定読者を記述）

# 記事のゴール
（この記事を読んだ読者が、どのような状態になることを目指すか記述）

# 出力形式
- Markdown形式
- 大見出し（h2）、中見出し（h3）で階層的に表現してください。
```

## 6. 執筆・公開フロー
記事の作成から公開までの基本的な流れは以下の通りです。

1.  **記事ファイルの作成**:
    ```bash
    npx zenn new:article
    ```
    - このコマンドで自動生成されたファイルを元に執筆を開始します。

2.  **執筆**:
    - 上記の「ファイル管理規約」「記事のメタ情報」「執筆スタイル・フォーマット」に従って記事を執筆してください。

3.  **プレビュー**:
    - ローカルで記事の表示を確認してください。
    ```bash
    npx zenn preview
    ```
    - ブラウザで `http://localhost:8000` にアクセスして確認できます。

4.  **Gitへのコミット**:
    - 記事ファイルの追加・変更が完了したら、Gitにコミットしてください。
    - コミットメッセージは、以下の規約を参考にしてください。
        - 新規記事の追加: `feat(article): 新規記事「〇〇」を追加`
        - 既存記事の修正: `fix(article): 記事「〇〇」の誤字を修正`
        - 記事の更新: `docs(article): 記事「〇〇」の内容を更新`

5.  **公開**:
    - `main` ブランチにマージされると、Zennによって自動的に記事がデプロイ・公開されます。
    - もしくは、手動で `npx zenn publish` コマンドを使用することも可能です。

---
